name: Build scummvm.a30

on:
  workflow_dispatch:
    inputs:
      scummvm_version:
        description: 'ScummVM version tag'
        default: 'v2026.1.0'
  workflow_call:
    inputs:
      scummvm_version:
        type: string
        default: 'v2026.1.0'

permissions:
  contents: write
  packages: write

env:
  IMAGE: ghcr.io/ruslan-k/scummvm-builder-a30

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Ensure latest release exists
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG="latest"
          gh release view "$TAG" >/dev/null 2>&1 || \
            gh release create "$TAG" \
              --target "${{ github.ref_name }}" \
              --title "Latest Build" \
              --notes "ScummVM ${{ github.event.inputs.scummvm_version || 'v2026.1.0' }} binaries for spruce" \
            || true

      - name: Download A30 toolchain
        run: |
          wget -q -O a30_toolchain-v1.0.tar.gz \
            "https://github.com/steward-fu/website/releases/download/miyoo-a30/a30_toolchain-v1.0.tar.gz"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.a30
          push: true
          tags: ${{ env.IMAGE }}:latest
          cache-from: type=registry,ref=${{ env.IMAGE }}:cache
          cache-to: type=registry,ref=${{ env.IMAGE }}:cache,mode=max

      - name: Restore ccache
        uses: actions/cache@v4
        with:
          path: ccache
          key: ccache-a30-${{ github.event.inputs.scummvm_version || 'v2026.1.0' }}-${{ github.sha }}
          restore-keys: |
            ccache-a30-${{ github.event.inputs.scummvm_version || 'v2026.1.0' }}-
            ccache-a30-

      - name: Build ScummVM
        run: |
          mkdir -p output ccache
          docker run --rm \
            -e SCUMMVM_VERSION=${{ github.event.inputs.scummvm_version || 'v2026.1.0' }} \
            -v ${{ github.workspace }}/output:/output \
            -v ${{ github.workspace }}/ccache:/ccache \
            ${{ env.IMAGE }}:latest

      - name: Upload Build scummvm.a30
        uses: actions/upload-artifact@v4
        with:
          name: Build scummvm.a30
          path: |
            output/scummvm
            output/scummvm.a30
            output/libfluidsynth.so.3*
            output/libsndfile.so.1*
            output/libglib-2.0.so.0*
            output/libgthread-2.0.so.0*
            output/libgobject-2.0.so.0*
            output/libgio-2.0.so.0*
            output/libgmodule-2.0.so.0*
            output/libffi.so.8*
            output/libtheoradec.so.1*
            output/libSDL2_net-2.0.so.0*
            output/fixjoy
          if-no-files-found: warn

      - name: Upload release assets
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if [ -f output/scummvm ]; then
            mv output/scummvm output/scummvm.a30
          fi

          gh release upload "latest" output/scummvm.a30 --clobber || true
          gh release upload "latest" output/libfluidsynth.so.3* --clobber || true
          gh release upload "latest" output/libsndfile.so.1* --clobber || true
          gh release upload "latest" output/libglib-2.0.so.0* --clobber || true
          gh release upload "latest" output/libgthread-2.0.so.0* --clobber || true
          gh release upload "latest" output/libgobject-2.0.so.0* --clobber || true
          gh release upload "latest" output/libgio-2.0.so.0* --clobber || true
          gh release upload "latest" output/libgmodule-2.0.so.0* --clobber || true
          gh release upload "latest" output/libffi.so.8* --clobber || true
          gh release upload "latest" output/libtheoradec.so.1* --clobber || true
          gh release upload "latest" output/libSDL2_net-2.0.so.0* --clobber || true
          gh release upload "latest" output/fixjoy --clobber || true
